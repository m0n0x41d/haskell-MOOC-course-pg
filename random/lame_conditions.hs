login user password = if user == "pepe"
                      then if password == "froggo"
                           then "pepe logged in"
                           else "wrong password"
                      else "unknown user"

